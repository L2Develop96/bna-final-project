//? This the Product model, you'll need it later when you start making API calls.
export interface IProduct {
  id: string;
  name: string;
  description: string;
  imgUrl: string;
  price: number;
}
